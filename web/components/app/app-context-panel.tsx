// The section-contextual panel. It carried a Book tree in the prototype, but that
// was mock data for the unbuilt Book feature, so the panel renders nothing for now
// (the work area takes the full width). Re-add per-section panels — from the
// prototype — as those features land.
export function AppContextPanel() {
  return null
}
