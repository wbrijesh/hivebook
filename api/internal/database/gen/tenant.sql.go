// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tenant.sql

package gen

import (
	"context"
	"database/sql"
	"encoding/json"
	"time"
)

const completeOnboarding = `-- name: CompleteOnboarding :one
INSERT INTO tenants (zitadel_org_id, name, size, region, use_cases, use_case_other, onboarded_at, updated_at)
VALUES ($1, $2, $3, $4, $5, NULLIF($6::text, ''), now(), now())
ON CONFLICT (zitadel_org_id) DO UPDATE SET
    name           = EXCLUDED.name,
    size           = EXCLUDED.size,
    region         = COALESCE(tenants.region, EXCLUDED.region),
    use_cases      = EXCLUDED.use_cases,
    use_case_other = EXCLUDED.use_case_other,
    onboarded_at   = COALESCE(tenants.onboarded_at, now()),
    updated_at     = now()
WHERE tenants.region IS NULL OR tenants.region = EXCLUDED.region
RETURNING id, zitadel_org_id, name, size, region, use_cases, use_case_other, onboarded_at, created_at, updated_at
`

type CompleteOnboardingParams struct {
	ZitadelOrgID string
	Name         sql.NullString
	Size         sql.NullString
	Region       sql.NullString
	UseCases     json.RawMessage
	UseCaseOther string
}

type CompleteOnboardingRow struct {
	ID           string
	ZitadelOrgID string
	Name         sql.NullString
	Size         sql.NullString
	Region       sql.NullString
	UseCases     json.RawMessage
	UseCaseOther sql.NullString
	OnboardedAt  sql.NullTime
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

// Region is write-once (ADR-0014), enforced atomically: the WHERE on DO UPDATE
// only applies when the stored region is unset or already equals the requested
// one. A conflicting region skips the update, so RETURNING yields no row — the
// caller maps that to ErrRegionImmutable. Race-safe with no partial write.
// onboarded_at is stamped once, then preserved.
func (q *Queries) CompleteOnboarding(ctx context.Context, arg CompleteOnboardingParams) (CompleteOnboardingRow, error) {
	row := q.db.QueryRowContext(ctx, completeOnboarding,
		arg.ZitadelOrgID,
		arg.Name,
		arg.Size,
		arg.Region,
		arg.UseCases,
		arg.UseCaseOther,
	)
	var i CompleteOnboardingRow
	err := row.Scan(
		&i.ID,
		&i.ZitadelOrgID,
		&i.Name,
		&i.Size,
		&i.Region,
		&i.UseCases,
		&i.UseCaseOther,
		&i.OnboardedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createTenantIfAbsent = `-- name: CreateTenantIfAbsent :exec
INSERT INTO tenants (zitadel_org_id)
VALUES ($1)
ON CONFLICT (zitadel_org_id) DO NOTHING
`

func (q *Queries) CreateTenantIfAbsent(ctx context.Context, zitadelOrgID string) error {
	_, err := q.db.ExecContext(ctx, createTenantIfAbsent, zitadelOrgID)
	return err
}

const getTenantByOrg = `-- name: GetTenantByOrg :one

SELECT id, zitadel_org_id, name, size, region, use_cases, use_case_other, onboarded_at, created_at, updated_at
FROM tenants
WHERE zitadel_org_id = $1
`

type GetTenantByOrgRow struct {
	ID           string
	ZitadelOrgID string
	Name         sql.NullString
	Size         sql.NullString
	Region       sql.NullString
	UseCases     json.RawMessage
	UseCaseOther sql.NullString
	OnboardedAt  sql.NullTime
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

// Tenant queries (design-doc 0005). Every Go-API SQL statement lives here and is
// consumed via the generated package (internal/database/gen); nothing else writes SQL.
func (q *Queries) GetTenantByOrg(ctx context.Context, zitadelOrgID string) (GetTenantByOrgRow, error) {
	row := q.db.QueryRowContext(ctx, getTenantByOrg, zitadelOrgID)
	var i GetTenantByOrgRow
	err := row.Scan(
		&i.ID,
		&i.ZitadelOrgID,
		&i.Name,
		&i.Size,
		&i.Region,
		&i.UseCases,
		&i.UseCaseOther,
		&i.OnboardedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
